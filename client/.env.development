# Development overrides — Vite proxies /api to the local Go server
VITE_APP_URI=http://localhost:8080
VITE_WS_ENDPOINT=ws://localhost:8080/ws
# Fill these in from the Terraform outputs for local dev against real AWS
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
