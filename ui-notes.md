# Notes

## Main Page

- Despite having AI Narration availalbe I'm getting the AI narration not availalbe message

## Create

- UX: Make a randomly assign button that just takes the stats and assigns them randomly
- UX (bug): The count when resetting assignments seems to keep increasing, leading to 7/6, 8/6, etc etc skills assigned. fix logic
- UX: Make sure all input boxes that are small have the autocomplete styling turned off like I had earlier. Also make
sure to use the new slotprops param instead of inputProps
- UX: Subrace selection preferably would be on the card. Make it clear that there are subraces, and then the player
can choose them after locking in elf for instance, it would transform to either types and reset if they chose another race.
