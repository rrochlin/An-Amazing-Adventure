# Terraform local cache and state — never commit
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Generated build artifacts
*.zip

# Local workspace overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Doppler local config (each developer sets up their own)
doppler.yaml

